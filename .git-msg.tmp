refactor(ui): 「? 快捷键」移到顶栏最右侧

- 从右上角动作组里独立出来，位于导出菜单右侧、与其它按钮之间加一条分隔线
  并拉开 8px 间距（帮助类入口离其它按钮远一点，避免误触）
- 端到端新增断言：既是右上角最后一个子元素，又必须是屏幕最右侧的按钮
  （同时按 DOM 顺序与 getBoundingClientRect 复核，避免只有其一成立）

验证：tsc 0 错 / 55 单测 / 25 自检 / 19 端到端 / 构建双哈希一致
