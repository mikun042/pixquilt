refactor(ui): 导入/导出收拢到右上角，导出折叠为菜单，模式切换位置不变

顶栏重新分组（按桌面软件习惯：文件操作在右上角，编辑操作靠左）：
- 左侧：品牌 + 模式切换（保持原位）+ 撤销/重做/重新转换/新建/? 快捷键
- 右上角：导入图片 + 导出菜单

导出菜单（图片 / 拼豆 / 数据 / 画布 四组）：
- 图片：PNG 1x、1x-20x 倍数一键、复制 PNG
- 拼豆：图纸 SVG + 缺口清单 CSV
- 数据：像素数据 JSON、项目 JSON
- 画布：新建空白画布
原先铺在顶栏的 9 个倍数按钮收进菜单，顶栏不再堆两行按钮。

配套改进：
- 顶栏结构只构建一次，render 只更新按钮可用状态
  （此前每次 render 重建顶栏，会把展开中的菜单一起冲掉）
- 撤销/重做按钮按实际可撤销步数禁用，不再永远可点
- 没有画布时导出按钮仍可点开，菜单里明确提示先做什么
- 新增 Ctrl+S = 导出 PNG 1x

验证：tsc 0 错 / 55 单测 / 25 自检 / 18 端到端（新增 3 条顶栏与菜单断言）/ 构建双哈希一致
