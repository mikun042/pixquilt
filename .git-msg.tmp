refactor(ui): 顶栏只保留模式切换在左侧，其余动作全部移到右上角

左侧：品牌 + 模式切换（位置固定不动，用户明确要求）
右侧：撤销 / 重做 / 重新转换 / 新建 / 快捷键 / 导入图片 / 导出菜单

- 动作按钮改为「图标 + 文字」结构，窗口 ≤1200px 时自动只显示图标
  （title 与 aria-label 保留完整说明，窄屏仍可辨识）
- 端到端断言同步更新：左侧不允许存在任何按钮、也不允许残留动作文案
  （逐词检查撤销/重做/重新转换/新建/导入/导出），防止以后又被加回左侧

验证：tsc 0 错 / 55 单测 / 25 自检 / 18 端到端 / 构建双哈希一致
