feat(ui): 取色器重做为 Blender 取色界面结构

按参考图（Blender 取色器）实现，替换原先的原生 <input type="color">：
- 圆形 HSV 色轮：中心白、外圈饱和；角度=色相、半径=饱和度，拖到圆外饱和度夹满
- 右侧竖向明度条（上亮下暗）+ 吸管按钮
- 透明度横条：拖到最左 = 选中「透明色」
- 顶部 RGB / HSV / Hex 标签切换数值行单位
- 色板分组：本图用色 / 最近使用 / 预置色卡（含拼豆号色）；点色块即取色
- 拖动中只预览、松手才提交（一次拖动 = 一条撤销），window blur 兜底收尾

同时修掉四条真实缺陷（均由本次改动或验证暴露）：
1. 取色器宿主元素原先随面板重建，实例 DOM 留在旧宿主上 → "打开正常、一拖动就消失"。
   改为模块级持久化宿主，渲染时只清色板子节点。
2. 零尺寸几何（侧栏收起/面板不可见）导致除零 → #NaNNaNNaN 写入主色。全路径加尺寸守卫。
3. setPointerCapture 在非活跃 pointerId 上抛 NotFoundError，位于处理器开头会打断整段取色。
   统一走 safeCapture()。
4. 透明度"拖到最左"判定用 <=0.001，亚像素取整后点不中（实测 0.006）。引入 6% 死区。
另修：取色器原先放在 renderPalette 的提前 return 之后，导致"还没导入图片时点主色块没反应"。

新增 tool/e2e-picker.mjs（11 项，含几何方向、标签切换、色板点选、透明度、零尺寸免疫），
并并入 npm run verify。

验证：tsc 0 错 / 55 单测 / 25 自检 / 19 端到端 / 11 取色器 / 构建双哈希一致
