fix: 修两个真实缺陷（导入后画布被空状态遮挡、量化缓存静默复用错误索引）

1) 导入图片"转换不出来"：空状态提示写死在模板里且自带不透明背景，导入成功后未移除，
   一直盖在画布上。改为按需渲染 + 有画布即移除，并加 e2e 断言"导入后空状态必须消失
   且画布真的画出内容"。

2) 固定色板下整幅图只剩一种颜色：量化缓存只校验截断键（每通道 5bit），不同颜色撞槽后
   复用了错误索引。改为槽位用截断键寻址、命中时校验完整 RGB。加单测：带缓存 quantize
   与独立实现的逐格最近色匹配逐位比对。

验证：tsc 0 错 / 55 单测 / 25 自检 / 15 端到端 / 构建双哈希一致
