// 补箭头：撤销（左端向下的三角）+ 重新转换（缺口处的三角）。
//
// 诊断依据来自 ASCII 输出：
//   · act_undo 是"拱形 + 两条向下的腿"，**没有箭头尖** → 看着像门把手
//   · act_regenerate 只有一条开口（左下），**右上没箭头** → 看着像 "C"
// 两个都缺"箭头"，而箭头是这两个图标表达语义的关键部件。
import { readFileSync, writeFileSync } from 'node:fs'

const p = new URL('./icons.mjs', import.meta.url)
let s = readFileSync(p, 'utf8')
const CRLF = String.fromCharCode(13, 10)
const LF = String.fromCharCode(10)
const had = s.includes(CRLF)
if (had) s = s.split(CRLF).join(LF)

const A = (lines) => 'A([\n' + lines.map((l) => `    '${l.padEnd(32, '.')}',`).join('\n') + '\n  ])'

function replaceFn(name, body) {
  const start = s.indexOf(`const ${name} = `)
  if (start < 0) throw new Error(`找不到 ${name}`)
  const end = s.indexOf('\n\n', start)
  if (end < 0) throw new Error(`找不到 ${name} 的结尾`)
  s = s.slice(0, start) + body + s.slice(end)
}

/* 撤销：顶部粗弧（横向"回溯"）+ **左端向下的实心箭头**。
   箭头画成"三角形"：上宽下尖，且**与弧的左端相接**（不接就散成两块）。
   右侧那条短腿是弧的回落，保留。 */
replaceFn(
  'undo',
  `const undo = () =>
  ${A([
    '................................',
    '................................',
    '................................',
    '................................',
    '................................',
    '..........##############........',
    '........##################......',
    '.......####################.....',
    '......#####################.....',
    '.....########.......########....',
    '.....######...........#####.....',
    '.....#####.............####.....',
    '.....####..............####.....',
    '.....####..............####.....',
    '.....####..............####.....',
    '.....####..............####.....',
    '.....####..............####.....',
    '.....####..............####.....',
    '.....####..............####.....',
    '.....####..............####.....',
    '....######..............###.....',
    '...########.....................',
    '..##########....................',
    '.############...................',
    '..##########....................',
    '...########.....................',
    '....######......................',
    '.....####.......................',
    '................................',
    '................................',
    '................................',
    '................................',
  ])}`,
)

/* 重新转换：闭合环（右上留缺口）+ **缺口处的实心箭头**。
   箭头要指向顺时针方向，与环的走向连续，才读得出"循环"。 */
replaceFn(
  'regenerate',
  `const regenerate = () =>
  ${A([
    '................................',
    '................................',
    '................................',
    '.............#######............',
    '...........###########..........',
    '.........###############........',
    '........#####.......######......',
    '.......####...........#####.....',
    '......####.............#####....',
    '.....####...............#####...',
    '.....###.................####...',
    '....####..................####..',
    '....###....................###..',
    '....###.........................',
    '....###.........................',
    '....###.........................',
    '....###.........................',
    '....###.........................',
    '....###.........................',
    '.....###........................',
    '.....####.......................',
    '......####......................',
    '.......####.....................',
    '........#####...................',
    '.........######.................',
    '..........########..............',
    '...........#########............',
    '.............########...........',
    '...............#####...........',
    '................................',
    '................................',
    '................................',
  ])}`,
)

writeFileSync(p, had ? s.split(LF).join(CRLF) : s)
console.log('撤销 / 重新转换 已补箭头')
