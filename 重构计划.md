# 像素画工作台 · 重构计划（v1 草案）

> 目标项目：`F:\像素画build`
> 旧项目：`F:\像素画制作`（**只读参照，本计划不改动它任何文件**）
> 本文件写在目标项目目录内，是本次交付的唯一产物。
> 状态：**待你确认 §13 的 6 个决策项后再开工**。
> 第一原则：**可维护性优先**——见 §15，它是本次重构的验收门，不是附带条款。

---

## 1. 摘要

用同一套已验证的算法与交互，重写为一个**依赖更小、模块边界更清、agent 接入更强**的单文件离线工具。

四句话概括这次重构：

1. **功能做减法**：保留 100% 的核心能力（像素化管线、精修编辑器、确定性导出），砍掉交互层的重复入口、只剩浅封装的 API、只在测试里用到的导出、以及指向已删功能的死文案。
2. **agent 接入做加法**：`window.pixelArtStudio` 契约**逐字保留**（现有 `batch.mjs`/`e2e.mjs` 只改 1 行路径即可继续跑），并补上旧项目一直缺的运行时自描述（`describe*()`）、机器可读汇总（`--json`）、参数校验预演（`--plan`）、以及**剥离 CDP 的 Node 端直调通道**（同一份 core 算法，无需浏览器）。
3. **架构改一处关键点**：把"算法"与"浏览器"彻底解耦为 `src/core`（零 DOM、零框架、Node 可 import），让批处理与页内走**同一份实现**——旧项目里 `batch.mjs` 有一大段 `page.evaluate` 模板字符串，逻辑与页内 render 两套写法，这正是最易漂移的地方。
4. **可维护性制度化**：单一真源（元数据即文档）、四层契约、可运行的验收门（哈希一致 / 黄金基准 / e2e）、以及"文档漂移会被测试抓住"的机制——详见 §15。

### 1.1 已确认的方向（本轮追加，覆盖原先的待确认项）

| 项 | 决定 | 对计划的影响 |
|---|---|---|
| 旧项目 | **完全不动**，只读参照 | §3 保持；不做改名/归档/删除（原 §13-F 关闭） |
| 动画 / 多帧 | **后续再加，本期不实现** | §6 数据模型**预留** `frames`，但 UI / API / 导出**一律不暴露**多帧概念（§4.7 约束） |
| UI | **由我重做**，不复刻旧布局 | 不再"先搬后用"；新增 **§16 UI 设计方案** |
| 项目目标 | **① 拼豆图纸设计 ② 独立游戏美术资产（AI 可批量生产）** | 新增 **§17 拼豆模式** / **§18 游戏资产模式**，两者进 P2 |
| 首要使用方式 | **agent 能驱动本项目产出像素图** | §8 的 L2/L3/L4 提前：**P0 就要有可跑的 CLI + 自描述** |

---

## 2. 目标目录与工具链（已实地确认）

| 项 | 实测结果 |
|---|---|
| `F:\像素画build` | **已存在且为空**（可写，已探针验证） |
| Node | v24.18.1 |
| npm / npx | 11.16.0 / 可用 |
| git | 2.55.0.windows.3 |
| 浏览器 | Edge `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`、Chrome `C:\Program Files\Google\Chrome\Application\chrome.exe` |

> 注意：`F:\像素画制作` 旁还有一份 `F:\像素画制作 - 副本`（旧备份）。**本次不读不改**，仅在此登记它的存在，避免误当新项目工作区。

**约束（来自你的指令）**：新项目只写在 `F:\像素画build`；除该目录外**禁止修改任何无关文件**。这意味着：
- 不动旧项目（含它的 `源码/`、`输出/`、文档）；
- 不动 `%APPDATA%`、注册表、全局 npm 包（依赖只装在新项目自己的 `node_modules`）；
- 不启动常驻服务；需要浏览器验证时用无头模式跑完即退出。

---

## 3. 与旧项目的关系

| 方面 | 处置 |
|---|---|
| 旧项目文件 | **只读**。作为行为基准（golden reference）与迁移对照 |
| 旧产物 `像素画工作台.html` | 保留不动，作为新旧同参对比的**输出基准** |
| 旧 API 契约 | **逐字继承**（见 §7），保证已有脚本/agent 提示词不失效 |
| 旧 CLI | 继承 22 个 flag 语义，**新增** 3 个（§8）；弃用 1 个 |
| 旧文档 | 不修改；新项目写自己的 `docs/AGENT_API.md` / `docs/USAGE.md`，内容按新契约重写 |
| 旧 bug/坏味道 | 不在旧项目修，在新项目规避（§5 逐条列出） |

---

## 4. 必要功能清单（重构后必须存在）

### 4.1 转换引擎（`src/core/pipeline.ts`）

| 能力 | 必要参数 | 备注 |
|---|---|---|
| 导入解码 | PNG/JPG(JFIF)/WebP/GIF 首帧/BMP/AVIF/ICO/SVG | SVG 按声明尺寸栅格化，长边夹 512–4096 |
| 居中裁剪 | `cropRatio: free \| 1:1 \| 4:3 \| 16:9` | |
| 多级降采样 | `downsample: average \| nearest`，逐级减半防摩尔纹 | 旧项目已验证的算法 |
| 预处理 | `brightness / contrast / saturation`（-100..100），在小图上做 | |
| 色板确定 | `auto`(Median Cut，K=2..64，>250k 格固定种子抽样) / `preset`(4 套) / `custom` | 上限 256 色 |
| 量化映射 | OKLab 最近色 + 直接映射表缓存（命中率<50% 自适应关闭） | |
| 抖动 | `none \| floyd(强度0-100) \| bayer(强度0-100)`，**必须在映射步内完成** | |
| 杂色清理 | 连通域 BFS，`cleanupMinSize 1..10`，**与抖动互斥** | |
| 真透明 | `alpha` 生成 `alphaMask`（0/255） | |
| 输出 | `{ width, height, indices: Uint8Array, palette: string[], alphaMask?: Uint8Array \| null }` | |

### 4.2 编辑算子（core 纯函数 + API 双层）

10 类：`fill` / `eraseColor` / `replaceColor` / `setCells` / `setAll` / `line` / `rect` / `ellipse` / `transform`(flipX/flipY/rotate90/180/270) / `trim`。

统一返回 `{ applied, changes: [{op, kind?, cells, changed}], ... }`，语义与旧项目一致（`changed` 是权威判定，含"只改 alpha"）。

### 4.3 编辑器界面

- 工具 6 个：画笔 / 选区 / 填充 / 吸管 / 矩形 / 椭圆（B/M/G/I/U/O）
- 笔刷 1×1 / 2×2 / 3×3；左键主色、右键背景色
- **透明色＝调色板首格（E）**：画笔/填充/形状/X 挖洞（懒建 alphaMask）
- 选区：M 框选（可从画布外起手、越界夹紧）、Shift 点选/框选、Ctrl+C/V、X/Del/Backspace 挖洞、F 填色、方向键移动（Shift ×10）、Esc 取消
- 撤销栈：50 帧 + 64MB 双上限
- 取色器：色轮 + 明度条 + RGB/HSV + Hex + 「色板最近色 + 用色板色」（拖动中直写 DOM，不触发整树重渲染）
- 色板面板：主/背景色（Hex 输入、点击开户取色器、⇄ 交换/Tab）、最近使用、工作色板（用量角标、右键菜单、⇅ 排序）、渐变行、自定义色板（增/删/清空/导入 .hex）
- 画布：滚轮锚点缩放、空格/中键平移、+ − 0、右下角视图浮层（适配/100%/±）、网格（每 8 格加强）、参考图层、悬停放大镜
- 参数面板：风格预设 + 自定义预设、像素数量（滑条+输入+15 档快捷）、降采样、裁剪、色板、抖动、清理、B/C/S、**透明处理（合并后的单一选择器，见 §5.1）**、显示开关、导出
- 状态栏：画布尺寸/工具/主背景色/笔刷或挖洞态/坐标/已选格数/已复制/缩放/编辑标记/文件名
- 响应式：≤1200px 收窄，≤980px 侧栏变抽屉（头部「色板/参数」开关）
- 可访问性：工具 `aria-pressed`、模态 `role=dialog` + 焦点管理、统一 `:focus-visible`、toast `aria-live`

### 4.4 持久化与项目文件

- **自动草稿**（IndexedDB）：防抖写入、启动恢复、原图单独存键、不可用降级提示、`DRAFT_VERSION` + sanitize
- **项目文件**：参数 + 色板 + 索引矩阵（base64）+ 可选 alpha + 版本号，**不含原图**，可分享；读取兼容 v1/v2，写出 v3（§6）
- **偏好**：localStorage（工具/颜色/显示开关/笔刷/透明态）+ 自定义预设

### 4.5 导出

PNG（整数倍 1–20 最近邻放大、单边≤16384 且面积≤64M 双上限自动降档、真 alpha / 单色键控两种透明）、复制 PNG 到剪贴板、色板 `.hex`、像素数据 JSON（**含每色用量表，拼豆原料清单**）、项目 JSON、`packSheet` 图集拼合（帧含真实 width/height）。

### 4.6 Agent / 批处理（本次重点强化，见 §8）

### 4.7 动画：本期"预留不实现"的具体边界

用户明确"动画功能后续再添加"。为了**将来加得进去、现在不背复杂度**，采取如下边界：

| 做法 | 具体 |
|---|---|
| 数据模型预留 | `PixelArt.frames?: Frame[]`（可选，`Frame = {indices, alphaMask?, durationMs?}`）**只写进类型与文档**；本期任何代码路径都不读写它 |
| API 预留 | `docs/AGENT_API.md` 标注 `renderFrames` / `frameCount` 为 **未实现（planned）**，`capabilities().animation === false` |
| 导出预留 | 图集 `packSheet` 已支持多张输入（等价于"逐帧导出后拼图集"），**动画素材本期就可用**：CLI 逐帧出图 + `--sheet` |
| 禁止事项 | 不引入时间轴 UI、不做 GIF/WebP 编码、不在 `edit()` 里加帧参数——避免半成品 API 冻结 |
| 将来接线点 | 一旦要做：`frames` → 时间轴 UI → `exportGif()` / `packSheet(frames)`；因为 `core` 是纯函数，动画层只加"帧循环"，不必改动管线与算子 |

**一句话**：本期把"单画布"做扎实，把"多帧"的**字段与文档位**留好，代码里不出现半成品。

---

## 5. 删除与合并清单（附理由）

### 5.1 交互层重复入口（合并后只留一处）

| 旧项 | 处置 | 理由 |
|---|---|---|
| 头部 `🖼 导出图片` 下拉（9 倍数 + 复制 PNG） | **删**，只留右侧「导出」区 | 与右侧完全等价（同一 `EXPORT_SCALES`/同一 handler），两套 9 项列表 |
| 帮助弹窗写「右键色块 → 全图替换为主色」 | **改**为「替换为…（可换主色/背景色/任一色板色）」 | 与实际 UI 不符的旧文案 |
| 帮助 + 使用说明写「勾选『提示色板最近色』」 | **删该句** | 该复选框早已删除，现在是常显提示行 |
| auto 取色档提示「去左侧色板面板加颜色」 | **改**为「切到『自定义』档后可在左侧编辑」 | 该区域只在 custom 档渲染 |
| 快捷键说明 4 处（左栏静态三行/帮助弹窗/两份文档） | **收敛为 1 处**：帮助弹窗为单一真源，左栏只留一句「按 ? 查看全部快捷键」 | 已实际漂移出上述两处错误 |
| 取色器 `✎ 屏幕吸管`（`EyeDropper`） | **删** | 仅 Chromium 支持，且"从画面取色"已由吸管工具 + Alt+点击覆盖 |
| 透明三控件（真 alpha 勾选 / 合成色 / 导出为透明勾选） | **合并**为「透明处理」单选：`不透明（合成到背景色）｜单色键控（导出时该色变透明）｜真 alpha（保留原图透明）` | 同一概念分散在两个面板三个控件，语义要靠读文档才懂 |

### 5.2 浅封装 / 重复实现（API 与 core 层）

| 旧项 | 处置 | 理由与迁移 |
|---|---|---|
| `ps.setPalette(colors)` | **删** | 函数体就是 `setParams({paletteMode:'custom', customPalette})`；`docs/AGENT_API.md` 记废弃项，agent 提示词改用 `setParams` |
| `ps.replaceColor(from, to?)` | **删**，能力合并进 `edit([{op:'replaceColor'}])` | 旧项目里 `App.handleReplaceColor`(34 行) 与 `api.replaceColor`(22 行) 是逐行同构的双实现，且行为已分叉（同色时 UI 弹提示、API 静默 0；色板满时 UI 提示、API 抛错）→ 统一为 core 一层纯函数 `replaceAllColors(art, from, to)` 返回 `{art, cells, reason}`，UI 只做提示、API 只做抛错 |
| `ps.convert()` | **保留** | 它是"无原图即报错"的守卫入口，语义清晰 |
| `ps.applyStylePreset` / `ps.stylePreset` | **保留** | 前者写状态、后者只读查询（批处理靠后者保证可复现），职责不同 |
| `ps.reset()` / `ps.newCanvas()` | **保留** | 前者回默认态、后者建真实画布并改 `longEdge` |
| `ps.setTool('eraser')` 兼容别名 | **删**，改为显式 `setEraseToAlpha(true)` | 已经是"上个版本"的兼容债；新项目无历史草稿，不必背 |

### 5.3 死代码（旧项目实测无任何引用，新项目不重建）

`luminance` 的多余导出、`downloadBlob` 导出、`buildProjectFile` 导出、`decodeSvg` 导出、`readLocalPresets`/`readLocalPrefs` 导出、`cloneArt`/`sameArt` 导出、`MAG_CELLS` 导出——旧项目里都只是**多余的 `export`**（同文件内有用）。新项目按"只在被跨模块引用时才 export"的规矩写。

### 5.4 CLI 层冗余

| 旧项 | 处置 |
|---|---|
| `--style <id>` 与 `--palette preset --preset <id>` 两套写法 | **合并**：`--palette` 直接接受 `auto \| pico8 \| gameboy \| nes \| cga \| *.hex \| #aabbcc,...`，删 `--style`、`--preset` |
| `--skip-existing`（跳过的是写出而非转换，名字有歧义） | **改名 `--if-missing`**，语义与文档一致 |

### 5.5 明确**不删**（已核实为有意设计，避免误伤）

`Del`+`Backspace` 双绑定、工具三入口（键/按钮/API）、`eraserToAlpha` 字段名语义、透明色不入 `palette[]`、用量表不含透明格、`render` 与 `importImage+convert` 双路径、`getInfo().canvasWidth` 等别名字段（新项目**合并为单一 `width` 并写进文档**，不再留同值双字段）。

---

## 6. 数据模型与迁移（新项目）

```ts
/** v3：字段瘦身 + 透明单一开关 */
interface ConvertParams {
  longEdge: number            // 8..2048，长边格数
  downsample: 'average' | 'nearest'
  cropRatio: 'free' | '1:1' | '4:3' | '16:9'
  paletteMode: 'auto' | 'preset' | 'custom'
  paletteK: number            // auto 档 2..64
  presetPaletteId: string     // 'pico8' | 'gameboy' | 'nes' | 'cga'
  customPalette: string[]     // ≤256
  dither: 'none' | 'floyd' | 'bayer'
  ditherStrength: number      // 0..100
  cleanup: boolean
  cleanupMinSize: number      // 1..10
  brightness: number; contrast: number; saturation: number
  matteColor: string          // 原 flattenBg：alpha 合成/键控色
  alpha: boolean              // 旧字段保留（=真 alpha），见下
}

interface PixelArt {
  width: number
  height: number
  indices: Uint8Array         // 行主序色板索引
  palette: string[]           // '#rrggbb'，≤256
  alphaMask?: Uint8Array | null  // 0=透明 255=不透明；null=全不透明
}
```

**透明态表示（对 agent 友好）**：参数里用 `transparent: 'none' | 'key' | 'alpha'` 作为**唯一开关**（UI 三控件合并后的产物），持久化时写 `transparent`，同时**写出等价的 `alpha` + `matteColor` 旧字段**，保证旧脚本/旧项目文件互操作。读取时 `alpha:true → 'alpha'`、`alpha:false + 导出键控 → 'key'`。

**项目文件**：`version: 3`，字段同旧 `ProjectFile`（`params/width/height/palette/indices(base64)/alpha?(base64)`），**读取兼容 v1/v2**，写出 v3。

**二进制伴生格式（新增，可选）**：`*.pixbin` = 12 字节头 + `indices` + 可选 `alphaMask`。用于大画布（2048² 的像素 JSON 约 40MB，base64 项目文件更大），agent 生成/读取更快。JSON 仍保留给人类与拼豆清单。

---

## 7. Agent 接口契约（`window.pixelArtStudio` v2）

> **原则**：**既有方法名/参数/返回形状逐字继承**（老脚本零改动），只做加法与新增强。`version` 升为 `'2.0'`，同时保留 `apiLevel: 2`。

### 7.1 转换与参数

| 方法 | 签名 | 说明 |
|---|---|---|
| `whenReady()` | `() => Promise<void>` | 草稿恢复完成后 resolve（脚本开头必调） |
| `getParams()` | `() => ConvertParams` | 副本 |
| `setParams(p)` | `(p: Partial<ConvertParams>) => ConvertParams` | 改参并**立即同步重转**（不弹确认），返回最终参数 |
| `applyStylePreset(id)` | `(id) => ConvertParams \| null` | 写状态 |
| `stylePreset(id)` | `(id) => Partial<ConvertParams> \| null` | **只读**查询预设 |
| `defaultParams()` | `() => ConvertParams` | 出厂默认（批处理的显式基底） |
| `importImage(src)` | `(File \| Blob \| dataURL \| URL) => Promise<{name,width,height}>` | |
| `convert()` | `() => {width,height,paletteSize}` | 无原图则 throw |
| `reset()` | `() => void` | 清空工作区与草稿（不弹确认） |

### 7.2 读取

`getInfo()`（`hasImage/imageName/imageWidth/imageHeight/hasArt/width/height/paletteSize/hasAlpha/hasEdits/tool/primary/bg/brushSize/eraserToAlpha/params`）、`getPalette()`、`getUsage()`、`hasAlpha()`、`countTransparent()`。

### 7.3 导出 / 载入

`exportPNG(scale=1, {transparentBg}?)` → dataURL；`exportPaletteHex()`；`exportPixelJSON()`；`exportProject()`；`loadProject(json)`；`copyPNGToClipboard`（页面内才可用）；`packSheet(items, {columns,padding,background})`。
**新增**：`exportPixBin()` → `Uint8Array`（大画布高速通道）、`importPixBin(bytes, params?)`。

### 7.4 后台编辑（声明式算子）

`edit(ops)`、`undo()`、`redo()`、`newCanvas({width,height,color?,transparent?})`、`render(src, params?, scale?, {transparentBg?, ops?})`（**无副作用**）、`renderBlank({width,height,color?,transparent?,ops?}, params?, scale?, {transparentBg?})`（**无副作用**）。

### 7.5 编辑器状态写入

`setTool(tool)`（`pencil|bucket|picker|rect|ellipse|selection`）、`setPrimary(hex)`、`setBg(hex)`、`setBrushSize(1|2|3)`、`setEraseToAlpha(on)`。

### 7.6 **新增：运行时自描述（这是旧项目最大的 agent 缺口）**

```js
ps.describe()          // 一次拿全：version/apiLevel/tools/ops/params/presets/exports
ps.describeOps()       // [{op, fields:[{name,type,required,default,desc}], desc}]
ps.describeParams()    // [{key,type,range/enum,default,desc}]
ps.validateParams(p)   // {ok, params, fixed:[{key,from,to,reason}]}  ← 干跑 sanitize，不落状态
ps.capabilities()      // {decodeFormats, maxSide:2048, maxExportSide:16384, maxPalette:256, ...}
```

**为什么必须有**：旧项目的算子表只写在 Markdown 里，agent 无法自发现；一旦文档与实现漂移，agent 只能靠试错。新项目把算子/参数元数据做成 `src/core/spec.ts` 的**单一常量**，同时驱动 ① `describeOps()` ② CLI `--help` ③ `docs/AGENT_API.md` 生成 ④ 单测断言"元数据算子名集合 == switch 实际处理集合"。**文档由代码生成，永不漂移。**

### 7.7 契约稳定性承诺

- 既有点位**不破坏**：新增能力一律加新方法/新字段；
- 错误一律 `throw Error`（中文原因），不返回 `null` 混用；
- **镜像同步**：`setParams/importImage/reset/loadProject` 之后，同一次 JS 调用内紧接读 `getInfo/getUsage/exportPNG` 即得最新值（不用等帧）；
- 确定性：同图 + 同参 + 同算子 = 同结果（同浏览器版本），可用 `artHash` 跨运行比对。

---

## 8. 新项目的 agent 接入矩阵（四层）

| 层 | 入口 | 适用场景 | 本次变化 |
|---|---|---|---|
| L1 页内 API | `window.pixelArtStudio` | AI 操作已打开的页面 / Playwright・CDP evaluate | 继承 + 自描述 + `exportPixBin` |
| L2 批处理 CLI | `tools/pix.mjs`（替代 `batch.mjs`） | 整套素材批量出图 | 保留 22 flag，`--json` / `--plan` / `--if-missing` 新增，`--style` 合并进 `--palette` |
| L3 **Node 端直调（无浏览器）** | `import { render } from 'pixbuild/core'` | agent 不需要 UI，只要结果；CI/服务器可用 | **新增**：core 纯函数不依赖 DOM；解码走 Node 侧适配器 |
| L4 自描述与预演 | `describe*` + `--help --json` + `--plan` | agent 冷启动自查、改参前预演 | **新增** |

> L3 的价值：现在一次批处理要"起浏览器 → CDP → 页面渲染 → dataURL 序列化 → 回传"，大图 payload 会绕一圈 base64。新项目让 `tools/pix.mjs` 能**优先走 L3**（`--in-process`），浏览器仅作解码兜底（`--browser-decode`）。这同时消除了旧项目里 `page.evaluate` 模板字符串与页内 render 的**双实现漂移风险**。

---

## 9. 技术选型

| 决策点 | 选择 | 理由 | 备选（§13-B） |
|---|---|---|---|
| UI 框架 | **无框架（vanilla TS）+ 极简响应式 store** | 编辑器主体是 `canvas` 2D + `pointer` 事件 + 直写 DOM（旧项目取色器拖动本来就是绕开 React 直写）；无框架让单文件产物更小、启动更快、重建成本更低 | 保留 React 18（迁移心理成本低，但产物更大） |
| 语言 | TypeScript（`strict`） | 类型是 core↔UI↔API 三方契约的安全网 | — |
| 构建 | Vite 5 + TS + 单文件内联（`vite-plugin-singlefile`） | 与旧项目同源，产出仍是**双击即用**的 `像素画工作台.html` | 无构建：原生 ESM 多文件（失去单文件分发） |
| 测试 | `esbuild` 打包 + `node:test` | Node 24 自带，esbuild 由 vite 传递依赖提供，零新增依赖 | vitest（多一个依赖） |
| 端到端 | 自写 CDP 客户端（继承旧 `cdp-lib.mjs` 思路，**修掉它曾经的定时器泄漏**） | 零依赖、可断言"真实鼠标/键盘" | Playwright（重依赖） |
| 依赖总量目标 | 运行期 **0 个**；构建期 3–4 个 | 离线工具不该靠 CDN 或运行时库 | — |

---

## 10. 目录结构（新项目）

```
F:\像素画build\
├─ 像素画工作台.html          # ★ 交付物：单个自包含 HTML（dist/index.html 的副本，哈希一致）
├─ package.json               # scripts: dev / build / test / typecheck / e2e / selftest
├─ tsconfig.json  vite.config.ts  .gitignore
├─ src/
│  ├─ core/                   # ★ 零 DOM、零框架、Node 可 import（本次架构核心）
│  │  ├─ types.ts             # ConvertParams / PixelArt / ProjectFile / 版本常量 + sanitize
│  │  ├─ spec.ts              # ★ 单一真源：算子与参数元数据（驱动 describe* / --help / 文档 / 测试）
│  │  ├─ color.ts             # hex↔rgb、HSV、OKLab、色值文字色、渐变
│  │  ├─ palettes.ts          # 预置色卡 + .hex 解析/序列化 + normalizeHex
│  │  ├─ pipeline.ts          # 降采样/预处理/MedianCut/量化/抖动/清理
│  │  ├─ ops.ts               # 10 类算子纯函数 + applyOps（含 changed 权威判定）
│  │  ├─ selection.ts         # 选区集合运算/剪贴板/移动/填充/挖洞（纯函数部分）
│  │  ├─ export.ts            # PNG 放大/像素JSON/项目JSON/pixbin/图集拼合/用量统计
│  │  └─ hash.ts              # artHash（跨运行比对）
│  ├─ app/                    # 浏览器专属：host 与平台适配
│  │  ├─ decode.ts  store.ts  clipboard.ts  storage.ts  prefs.ts  toast.ts
│  │  ├─ ui/                  # header / toolbar / palette / params / canvas / statusbar / help / drawers
│  │  └─ automation.ts        # window.pixelArtStudio 与 core 的唯一粘合层（薄）
│  ├─ test/                   # core.test.ts / ops.test.ts / cli.test.ts / spec.test.ts
│  └─ style.css
├─ tools/
│  ├─ pix.mjs                 # 批处理 CLI（L2），纯函数部分可被单测 import
│  ├─ cdp.mjs                 # 极简 CDP 客户端 + 浏览器探测（含定时器清理）
│  ├─ node-decode.mjs         # Node 侧解码适配器（PNG 优先，其余回退 CDP）
│  ├─ e2e.mjs                 # 真实鼠标/键盘端到端回归
│  └─ fixtures/               # 测试用自制小图（不引用旧项目素材）
├─ docs/
│  ├─ AGENT_API.md            # ★ 由 core/spec.ts 生成的接口文档（含算子表）
│  ├─ USAGE.md                # 用户向：参数表 + 快捷键 + FAQ
│  └─ ARCHITECTURE.md         # 分层、数据模型、性能基准、决策记录（ADR）
├─ dist/                      # 构建产物（.gitignore）
└─ 重构计划.md                # 本文件
```

**一处工程改进**：CLI 找工作台 HTML 不再像旧项目那样写死 `join(here,'..','..','像素画工作台.html')`（该路径依赖目录形状，一挪就断）。新规则：`--app` 显式 > `<repo>/dist/index.html` > `<repo>/像素画工作台.html`，并在 `--selftest` 里断言能找到。

---

## 11. 实施阶段（每阶段可独立验收，建议按序）

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| **P0 骨架** | 仓库初始化（`git init` + `.gitignore`）、工程配置、`core/types.ts` + `sanitize`、空 UI 壳、构建链路 | `npm run build` 产出单文件 HTML，双击可打开；`typecheck` 0 错 |
| **P1 引擎** | `pipeline.ts` / `palettes.ts` / `color.ts` | 单测：与旧项目**同图同参输出逐位一致**（用旧 HTML 生成基准数据比对） |
| **P2 算子与导出** | `ops.ts` / `selection.ts` / `export.ts` / `hash.ts` | 单测：10 类算子语义、`changed` 判定、trim/rotate 边界、用量口径（不含透明格） |
| **P3 UI** | `app/ui/*` 全部面板与画布交互、store、快捷键 | `e2e.mjs` 真实鼠标/键盘：画笔拖动、Ctrl+Z、框选越界、透明挖洞、抽屉布局、无障碍属性全绿 |
| **P4 Agent 层** | `automation.ts`（§7 全量契约）、`spec.ts` → `describe*` / 文档生成、`tools/pix.mjs`、`tools/cdp.mjs` | `--selftest` 全绿；**旧项目的 `batch.mjs` 只改 `--app` 路径即可跑通新产物**（兼容性铁证） |
| **P5 Node 直调** | `node-decode.mjs` + `--in-process` 路径 | 无头浏览器关掉也能出图；2048² 耗时 ≤ 旧项目基线 |
| **P6 文档与收尾** | `AGENT_API.md`（生成）、`USAGE.md`、`ARCHITECTURE.md`、性能基准表、首次 git 提交 | 文档与 `describe()` 输出逐条一致；三份文档无矛盾 |

**贯穿纪律**（继承旧项目已被验证有效的"铁律"）：
1. 改 `src/` 后必须 `npm run build` 并**同步** `像素画工作台.html`，两文件哈希逐位一致；
2. 验证链：`typecheck` → `test` → `build+同步` → `--selftest`，四项全绿才算完成；
3. 纯函数优先：新算法进 `core/` 并配单测，UI 只做编排；
4. 注释写"为什么"，中文；
5. 每阶段单独 git 提交，便于回退。

---

## 12. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 算法重写导致像素结果与旧版有细微差异 | 用户"感觉画质变了" | P1 用旧产物做**黄金基准比对**（同图同参逐位一致）；差异只允许来自**有意变更**（如透明合并），并逐条列出 |
| 无框架重写 UI 工作量大 | 周期拉长 | UI 层**先搬后用**：结构照旧项目（已验证的交互与文案），只换渲染方式；必要时保留 React（§13-B） |
| 旧脚本依赖被删的点位（`setPalette`/`replaceColor`/`setTool('eraser')`/`--style`） | 已有 agent 提示词失效 | **默认保留一版 deprecated 别名**（打 console 警告），并在 `AGENT_API.md` 标废弃与替代写法 |
| `F:\像素画制作 - 副本` 与新项目混淆 | 改错目录 | 所有工具只在 `F:\像素画build` 下读写；脚本内路径一律 `import.meta.url` 推导，不写绝对路径 |
| 沙箱：目标目录在工作区外 | 每次写入需授权 | 已确认可写；后续每阶段首次写入若被拒，按规则一次性申请即可 |
| **文档与实现漂移**（旧项目已实际发生：帮助文案指向已删控件） | agent 与用户被误导 | §15 的"元数据即文档 + 漂移检测测试" |

---

## 13. 待你确认的决策项

| # | 决策 | 我的建议 | 备选 |
|---|---|---|---|
| **A** | 已删点位的兼容别名要不要留一版？ | **留**（`setPalette`/`replaceColor`/`setTool('eraser')`/`--style` 打警告保留一个版本） | 直接删干净（更清爽，但旧提示词需同步改） |
| **B** | UI 用无框架 vanilla TS 还是保留 React 18？ | **vanilla TS**（产物更小、启动更快、贴合 canvas+直写 DOM 的实现方式） | React 18（迁移成本低，产物更大） |
| **C** | 是否新增 Node 端直调（L3，无浏览器出图）？ | **做**，排在 P5（先做稳契约与 UI） | 不做，批处理始终走无头浏览器 |
| **D** | 是否需要 gif/webp 动图与多帧动画？ | **不做**（旧项目也是单画布；动画素材用 `packSheet` 出图集） | 做多帧编辑（工作量最大，建议独立立项） |
| **E** | 新项目是否 `git init` 并分阶段提交？ | **做**（旧项目 init 了却零提交，是本次审阅发现的实际风险） | 不建仓库（纯文件交付） |
| **F** | 旧项目后续如何处置？ | **原样冻结**，等新项目 P4 验收通过后再议 | 作为 legacy 归档，或删除 |

---

## 14. 下一步

你确认 §13 的 6 项（或直接说"按建议来"）后，我从 **P0 骨架** 开工：在 `F:\像素画build` 建立工程、跑通 `typecheck + build`，把单文件 HTML 产出并双击验证，然后逐阶段推进，每阶段给你可验证的结果（含实测数值与失败项如实报告）。

如需先看效果，也可以只让我做 **P0+P1**（骨架 + 引擎黄金基准比对），确认画质与旧版逐位一致后再继续 UI。

---

## 15. 可维护性设计（本次重构的验收门）

> 你说"务必保证后续可维护性"——所以这一节不是补充说明，而是**约束前面所有章节的规则**。
> 判定标准：一个**没有参与本次重构的人（或 AI）**，在只读 `docs/` 与代码的情况下，能在 30 分钟内
> ①locate 任一功能的实现、②安全地改一处行为、③知道改完必须跑哪些命令、④被机器告知是否改坏了。

### 15.1 五条不可妥协的结构规则

| # | 规则 | 为什么 | 如何被强制执行 |
|---|---|---|---|
| R1 | **依赖单向**：`core`（零 DOM/零框架）← `app` ← `automation`；`tools` 只依赖 `core` | 旧项目 `core` 里混了 `exporter` 的 DOM 下载与 `color` 的 UI 色彩，导致"能不能单测"要靠人记 | ESLint `no-restricted-imports` 规则 + `typecheck`；CI 脚本断言 `core/**` 不含 `document.`/`window.` |
| R2 | **单一真源**：参数与算子的元数据只在 `core/spec.ts` 定义一次 | 旧项目同一份算子表在文档与代码各写一遍，已经漂移 | 单测断言"spec 算子集 == switch 实际处理集 == 文档表"，任一不符则红 |
| R3 | **纯函数优先**：新算法先写 `core` 纯函数 + 单测，UI 只做编排与提示 | 旧项目已验证这条最省事（可回归、可基准比对） | Code review 检查项 + `core/` 覆盖率门槛 |
| R4 | **对外契约集中**：`window.pixelArtStudio` 只在 `app/automation.ts` 出现，且只做参数校验/状态同步/调用 core | 旧项目 API 里内联了几十行业务逻辑（replaceColor 就是），与 UI 重复 | 该文件行数上限（建议 ≤350 行）+ 单测直打契约 |
| R5 | **无隐式全局**：不使用 `window.*` 之外的隐式状态；跨模块通信走显式 store 订阅 | 旧项目有 `hoverBus`/`uiBus` 两个自制事件总线，命名与职责靠注释维持 | 目录约定 + review |

### 15.2 四次"防漂移"机器检查（写进 `npm test`）

1. **元数据一致性**：`spec.ts` 的算子/参数集合 == `ops.ts` switch 处理集合 == `docs/AGENT_API.md` 表格行集合。
2. **文档新鲜度**：`docs/AGENT_API.md` 由 `npm run docs` 生成；测试比对"重新生成后与仓库内文件逐字节一致"，不一致即失败（旧项目正是靠人工同步三份文档，结果漏了）。
3. **产物同步**：构建后校验 `dist/index.html` 与根目录 `像素画工作台.html` 的 SHA256 一致（旧项目的"铁律 1"，现在变成脚本而非口头纪律）。
4. **UI 文案存在性**：帮助弹窗与提示文案里引用的控件 id 必须在 `spec`/UI 注册表里存在——这条能直接拦住旧项目那类"文档让用户勾选一个已被删除的开关"。

### 15.3 代码层面的可维护性约定

- **模块规模上限**（软门，超过需在 review 说明理由）：单文件 ≤ 400 行；单函数 ≤ 60 行；`core/` 单文件 ≤ 500 行。
  （对照旧项目：`App.tsx` 974 行、`pixelate.ts` 1085 行、`batch.mjs` 664 行——重构后按职责切开）
- **命名**：文件名与导出名一一对应；`useXxx` 之类的框架命名不再使用（无框架）；纯函数动词开头、谓词返回 boolean 用 `is/has/can`。
- **注释只写"为什么"**：不复述代码在做什么；涉及算法取舍（如"抖动必须并入量化步""清理与抖动互斥"）必须留注释并指向 `docs/ARCHITECTURE.md` 的对应小节。
- **错误信息**：面向使用者与 agent 的中文原因（旧项目这条做得好，继承），且**同一种错误只用一种措辞模式**。
- **不引入隐式约定**：所有"魔法数字"（50 帧撤销上限、64MB、16384、2048、256 色、ALPHA_THRESHOLD=128）集中在 `core/limits.ts`，并在文档里各有一处解释。

### 15.4 测试作为可维护性的基础设施

| 层 | 工具 | 覆盖目标 | 运行成本 |
|---|---|---|---|
| 单元（core） | `node:test` + esbuild | 管线各步、10 类算子、sanitize、导出格式、`spec` 一致性 | < 3s，无需浏览器 |
| 契约（API） | 无头浏览器 + `tools/cdp.mjs` | `window.pixelArtStudio` 全部点位与错误路径（`--selftest`） | ~10s |
| 交互（e2e） | 同上，真实 `Input.dispatchMouseEvent/KeyEvent` | 画笔拖动、Ctrl+Z、框选越界、透明挖洞、抽屉、无障碍 | ~20s |
| 基准（回归） | `tools/bench.mjs` | 2048² 各算子耗时与旧项目基线对比，超阈值告警 | 可选、手动跑 |

**"改坏即被抓住"的实证要求**（继承旧项目的验证方法）：每一项关键断言都要演示一次"故意改坏 → 测试变红 → 改回 → 变绿"，并把结果写进提交说明。这是判断测试是否真有价值的标准，而不是看数量。

### 15.5 文档体系与维护节奏

| 文档 | 读者 | 真源 | 更新时机 |
|---|---|---|---|
| `docs/AGENT_API.md` | AI agent / 脚本作者 | **由 `spec.ts` 生成** | `npm run docs`，测试保证不漂移 |
| `docs/USAGE.md` | 最终用户 | 手写 | 改参数/快捷键/导出行为时 |
| `docs/ARCHITECTURE.md` | 后续维护者 | 手写 + ADR | 每次结构决策后追加一条 ADR（含背景/选项/结论/后果） |
| `README.md` | 进入项目的人 | 手写，≤80 行，只放"怎么跑 + 去哪看" | 入口变化时 |
| `CHANGELOG.md` | 所有人 | 手写 | 每个阶段/版本 |

### 15.6 版本与演进纪律

- **版本三线**：`package.json version`（工程）｜`apiLevel`（页内 API 契约，整数，只增）｜`SCHEMA_VERSION`（项目文件）。
- **废弃流程**：标记 deprecated（代码注释 + `AGENT_API.md` 表格"废弃"列 + 运行时 `console.warn` 一次）→ 至少保留一个大版本 → 移除时在 `CHANGELOG.md` 写明替代写法。
- **提交纪律**：`feat:` / `fix:` / `refactor:` / `docs:` / `test:` 前缀；每个提交必须处于"验证链四项全绿"状态；每个阶段结束打 tag。
- **不做的"优化"**：不为赶进度引入新框架/新依赖；不重写已通过黄金基准比对的算法；不为"以后可能用到"预留抽象（旧项目的 `render`/`renderBlank`/`edit` 三入口就是因为场景不同才各自成立，不合并）。

### 15.7 可维护性的验收清单（P6 结束时逐条打勾）

- [ ] 新人/新 agent 能只靠 `docs/` 跑通：`npm install && npm test && npm run build && node tools/pix.mjs --selftest`
- [ ] `core/` 零 DOM：静态检查通过，且 `core` 单测**不启浏览器**即可全绿
- [ ] 四条防漂移检查全部在 `npm test` 里；任一条被故意破坏都能让测试变红（有演示记录）
- [ ] 每个 `docs/` 文件都有明确的"何时更新"说明；`AGENT_API.md` 为生成物、有生成命令
- [ ] 所有魔法数字集中在 `core/limits.ts` 且各有一句解释
- [ ] 单文件行数上限达标（无 >400 行的源码文件，`core` 无 >500 行）
- [ ] 提交历史有阶段 tag，且每个提交都过验证链
- [ ] `window.pixelArtStudio` 契约有对应单测/自检项；`describe()` 输出与文档逐条一致
- [ ] 性能基准表存在且可在本机复现（`tools/bench.mjs`）
- [ ] 旧项目脚本（`batch.mjs`）改路径后能在新产物上跑通（向后兼容实证）

---

## 16. UI 设计方案（重做，不复刻旧布局）

### 16.1 设计目标（服务于两个真实场景）

| 场景 | 用户 | 对 UI 的要求 |
|---|---|---|
| 拼豆图纸 | 手工爱好者，常对着屏幕数格子 | **大画布、清晰网格、每色用量与编号可见**、能按"号色 + 数量"读 |
| 游戏资产 | 独立开发者，批量出图 | **参数一次调好、批量复用**、导出规格明确（尺寸/锚点/透明）、少点击 |
| agent 驱动 | AI 无 UI | **UI 不挡 API**：任何 UI 操作都有对应 API；UI 只做"给人用"的那一层 |

### 16.2 布局（三区 + 两个可折叠抽屉）

```
┌──────────────────────────────────────────────────────────────────────┐
│ 顶栏：品牌 · 新建/导入 · 重做/撤销 · [模式：图片→像素｜拼豆｜游戏资产]  │
│       · 导出 ▾（PNG/图集/拼豆清单/项目 JSON）· 帮助 ?                 │
├───────────┬──────────────────────────────────────────┬───────────────┤
│ 左栏(工具) │              画布（自适应居中）             │ 右栏(参数)     │
│ 工具 6 个  │   网格（每 8 格加强）/ 棋盘透明底            │ 模式专属参数   │
│ 笔刷 1-3   │   右下角浮层：适配 · 100% · −/+ · 网格开关   │ 风格预设       │
│ 主/背景色  │   左上角：画布尺寸 · 缩放 · 当前工具          │ 调色板         │
│ 最近使用   │                                          │ 导出设置       │
│ 调色板     │   底部状态栏：坐标 · 已选 · 用量合计 · 文件名  │               │
└───────────┴──────────────────────────────────────────┴───────────────┘
```

**与旧布局的关键差异（不是换皮，是解决具体问题）**：

1. **顶部显式的"模式"切换**（`图片→像素` / `拼豆` / `游戏资产`）：同一个引擎、三套参数面板，避免现在"所有参数堆在一个长面板里"。
2. **调色板从独立面板变成"色带 + 抽屉"**：旧布局把色板塞在左栏最下方，工作色板到 256 色时要滚很久；新设计主色带常驻，完整色板/自定义编辑进抽屉。
3. **用量处处可见且可切换口径**：色块角标支持 `格数 / 珠子数 / 重量(g)` 三种显示（拼豆用户按"袋"买），旧项目只有"格数"。
4. **导出收敛为一个菜单**（旧项目头部菜单与右侧面板是两套等价入口）：`PNG(1x–20x) / 透明底 / 图集 + 坐标表 / 拼豆清单(csv+svg) / 项目 JSON`。
5. **取色器改为内联可折叠面板**（不再强制占满左栏），并在拖动时只更新自己，不引起画布重绘。
6. **键盘优先**：所有工具/动作都有快捷键，帮助面板由 `spec.ts` 生成（永不与实际不符）。

### 16.3 无框架渲染策略（保证可维护 + 不掉帧）

- 一个 **`store.ts`**：`state` + `subscribe(key, fn)` + `patch(partial)`，按 key 精确通知（避免旧项目"鼠标每跨一格重渲染整个应用"的问题）。
- **画布与取色器直写 DOM/canvas**，不经过 store 通知链；store 只承载"结构性状态"（工具、参数、色板、选择集）。
- **面板渲染函数是纯的**：`renderPanel(state) → HTMLElement`，只订阅自己关心的 key；单文件 ≤400 行（§15.3）。
- 所有 UI 动作与服务端能力一一对应到 `automation.ts`，UI 不直接改 core 状态。

### 16.4 可访问性与响应式（不降级）

工具按钮 `aria-pressed`、模态 `role=dialog` + 焦点移入/还原、`:focus-visible` 统一、toast `aria-live`；≤1200px 收窄侧栏，≤980px 侧栏变抽屉（顶栏按钮开关）。

---

## 17. 拼豆图纸模式（Bead Mode）

> 这是"项目目标 ①"。核心洞察：**拼豆用户的最终产出不是 PNG，而是一张能照着摆的图纸 + 一张采购清单**。

### 17.1 引擎侧能力（不需要新算法，需要"新口径"）

| 能力 | 实现要点 |
|---|---|
| **固定号色板** | 拼豆品牌色卡（如 Artkal/Hama 常用号）作为 `paletteMode: 'preset'` 的一种；`.hex` 文件可导入自建色卡 |
| **强制保留色板** | 量化阶段**不允许**新增颜色（旧项目在"色板满"时会退化为最近色并扩充），拼豆模式的唯一要求是"只能用我已有的号色" |
| **色号索引** | 色板项可携带 `code`（如 `S12`/`H05`），图纸与清单按号色排序输出 |
| **用量口径** | `格数 → 珠子数`（1 格 = 1 珠）+ `估算重量`（珠径/单珠重量可配，默认 5mm/约 0.08g） |
| **分板切分** | 按常用板规格（29×29 大板）把画布切成多张板图，给出每板的行/列范围与每板用量 |
| **图纸导出** | ① `图纸.svg`：每格填色 + **号色标注**（小格子用数字/符号缩写，超过阈值自动只看网格+图例）+ 十格加强线 + 图例表 ② `缺口清单.csv`：`号色, 颜色, 格数, 珠数, 估算重量, 建议袋数` |
| **可选** | 镜面/上下翻转出图（熨烫时用）、"只导出用到的号色" |

### 17.2 为什么放 P2

拼豆模式的**全部价值都在导出与口径**，不涉及新算法：管线复用，新增 `bead.ts`（纯函数：切板、统计、SVG/CSV 生成）+ `docs/BEADS.md`。等引擎（P1）与导出（P2）稳了，是"加一层输出格式"的工作量。

### 17.3 验收标准

- 给定一张图 + 16 色号色板 → 输出的 `缺口清单.csv` 中**每色格数之和 == 画布格数**（含透明格单列一行），且**没有任何色号不在输入色板内**；
- `图纸.svg` 在浏览器与 Inkscape 中打开视觉一致，格子与图例对齐；
- 分板切分：所有板拼起来恰好覆盖画布、无重叠无遗漏（单测断言格集合的并集与交集）。

---

## 18. 游戏资产模式（Game Asset Mode）

> 这是"项目目标 ②"。核心洞察：**引擎要的不是一张漂亮的 PNG，而是一组尺寸对齐、锚点明确、命名可预测的文件**。

### 18.1 引擎侧能力

| 能力 | 实现要点 |
|---|---|
| **尺寸预设** | 16×16 / 24×24 / 32×32 / 48×48 / 64×64 / 128×128；**强制输出精确尺寸**（不像自由模式按长边推短边） |
| **对齐而非裁剪** | 每帧画布尺寸固定，内容居中/贴底对齐由 `anchor` 决定（`center` / `bottom-center` / `top-left`），导出时**不裁边**（旧项目 `trim` 会让帧尺寸不一，引擎侧要额外处理） |
| **锚点元数据** | 帧记录含 `offsetX/offsetY`（内容相对画布中心的偏移），供引擎 pivot 使用 |
| **调色板一致性** | 一批资产共用一套色板（强制 `custom`/`preset`，禁用 `auto`），保证整套风格统一 |
| **命名模板** | `{name}_{w}x{h}`、`{group}`、`{index:02}` 等占位符（继承旧项目 `applyTemplate`，并加零填充） |
| **引擎导出格式** | ① **Godot 3/4** `SpriteFrames .tres`（区域/帧列表） ② **Unity** 图集切片 JSON ③ **Tiled/Phaser/Aseprite** 通用 `spritesheet.json`（`frames[] = {name, x,y,w,h, offsetX,offsetY}`） ④ **纯图集** PNG + 坐标表 JSON |
| **批量** | 一条命令：目录 → 逐张出图 + 按 glob 分组出图集 + 各格式元数据；`--manifest` 每张一套参数 |
| **可复现** | 全部参数可打印（`--json` 回显最终参数），同输入同输出（`artHash` 比对） |

### 18.2 与旧项目 `packSheet` 的差异（为什么要重做）

旧项目 `packSheet` 只输出"图集 + 每帧 x/y/宽/高"，且帧宽高**因 trim 而不同**；新实现：
- 帧尺寸**恒等**（不 trim），`offsetX/offsetY` 表达内容偏移 → 引擎侧直接可用，无需二次计算；
- 输出**多引擎元数据**，而不只是一个坐标表；
- 拼图算法从"逐行摆放"升级为 **skyline/最大矩形装箱**（减少空白面积），并断言"任意两帧矩形不相交"。

### 18.3 验收标准

- 给定 8 张 32×32 素材 → 输出图集 + 三份元数据，**所有帧矩形互不相交且不越界**（单测断言）；
- 元数据能被对应引擎/工具真实读取（Godot `.tres` 用文本格式，可用结构断言；Tiled JSON 按 schema 断言）；
- 同输入两次运行 `artHash` 一致（可复现）。

---

## 19. 优先级调整（相对 §11）

| 阶段 | 调整 |
|---|---|
| **P0 骨架** | 追加要求：**agent CLI 必须可用**（L2/L3 最小集：`--in/--out/--palette/--scale/--alpha/--json/--selftest`），因为"agent 能产出像素图"是本项目首要使用方式 |
| **P1 引擎** | 不变 |
| **P2 导出** | 追加 §17 拼豆导出与 §18 游戏资产导出；`trim` 保留（拼豆/精灵单图用），但游戏资产模式**默认不裁** |
| **P3 UI** | 按 §16 重做（不再"先搬后用"） |
| **P4 Agent** | 自描述 + 文档生成；`--sheet`/`--manifest` 在此补齐 |
| **P7 动画（新增，最末）** | 用户确认后再立：多帧编辑 + GIF/WebP 导出 + 时间轴 UI |
